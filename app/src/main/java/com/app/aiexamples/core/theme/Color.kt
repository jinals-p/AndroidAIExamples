package com.app.aiexamples.core.theme

import androidx.compose.ui.graphics.Color


// ── Brand / Accent ────────────────────────────────────────
val AccentPurple  = Color(0xFF7C6AFF)   // primary purple
val AccentViolet  = Color(0xFF9B59FF)   // lighter purple
val AccentBlue    = Color(0xFF4D8AFF)   // blue accent
val AccentPink    = Color(0xFFFF6A9E)   // pink highlight
val AccentGreen   = Color(0xFF4ADE80)   // success green
val AccentTeal    = Color(0xFF6AFFDA)   // teal accent
val AccentOrange  = Color(0xFFFFB347)   // warning orange

// ── Background Layers (dark blue/purple) ─────────────────
val Background    = Color(0xFF080B1A)   // deepest — main bg
val Surface       = Color(0xFF0D1128)   // header, bottom bar
val Card          = Color(0xFF111535)   // cards, bubbles
val CardAlt       = Color(0xFF161A3D)   // elevated cards

// ── Borders ───────────────────────────────────────────────
val Border        = Color(0xFF252A52)   // subtle border
val BorderBright  = Color(0xFF3D4494)   // focused border

// ── Text ─────────────────────────────────────────────────
val TextPrimary   = Color(0xFFE8E8FF)   // slight blue tint on white
val TextSecondary = Color(0xFFAAAAAF)   // secondary text
val TextMuted     = Color(0xFF5A5F8A)   // placeholder, hints

// ── Status ────────────────────────────────────────────────
val TextGreen     = Color(0xFF4EB28B)
val TextRed       = Color(0xFFFF6B6B)
val TextYellow    = Color(0xFFFFD166)

val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)